Your task is to define a custom crossover function with the DEAP framework. This is because evolution in the tree is stagnating. Here is the current logbook of progress:

INSERT_LOGBOOK_HERE

Write a Python function called 'crossover_individuals(individual1, individual2, pset)' that returns two offpspring of a crossover operation with the two original individuals. 
Return the a tuple of the offspring as ind1, ind2. Both individuals are of type gp.Individual. Do not return any other objects or texts.

Include all necessary imports, including random.

Assume 'individual1' and 'individual2' are a DEAP gp.PrimitiveTree representing GP individuals, and 'pset' is the DEAP PrimitiveSet used to generate it. Do not convert the types of each individual.

You may define additional parameters if needed and document them in comments.

The function should crossover parent trees **safely**, respecting their type:
- Nodes can be Primitives (functions), Terminals (variables/constants), or Ephemeral constants.
- The LLM has freedom to design the crossover strategy, as long as the resulting individuals are valid and can be used with DEAP GP algorithms.
Use only gp.PrimitiveTree, gp.Primitive, gp.Terminal, and standard Python operations on the tree list.

Do NOT import or reference any DEAP APIs that are not explicitly documented.
Do NOT create GP nodes manually (__class__(), constructors, or factories are forbidden).
The final individuals MUST be a valid gp.PrimitiveTree.
Do NOT assume the existence of classes like EphemeralConstant, PrimitiveTree, or helper functions such as get_leaves.
All replacement nodes must be selected from pset.primitives or pset.terminals.
The tree structure (prefix order and arity consistency) must remain valid.
Ephemeral constants must be handled by selecting from pset.terminals, not by instantiation.
Do not assume numeric operations on non-numeric nodes. Do not use any DEAP methods that do not exist.
Do not convert individuals to lists using list() or slicing.
Do not recreate the tree structure manually.

Generate Python code that can run immediately with DEAP, using proper imports and variable names. Ensure there are no undefined variables or typos. Check consistency of all variable names.
Include this line at the top of the method: from deap import base, creator, tools, gp

An example crossover method is given below:
        
from collections.abc import Sequence
from itertools import repeat
from deap import base, creator, tools, gp

if len(ind1) < 2 or len(ind2) < 2:
    # No crossover on single node tree
    return ind1, ind2

# List all available primitive types in each individual
types1 = defaultdict(list)
types2 = defaultdict(list)
if ind1.root.ret == __type__:
    # Not STGP optimization
    types1[__type__] = list(range(1, len(ind1)))
    types2[__type__] = list(range(1, len(ind2)))
    common_types = [__type__]
else:
    for idx, node in enumerate(ind1[1:], 1):
        types1[node.ret].append(idx)
    for idx, node in enumerate(ind2[1:], 1):
        types2[node.ret].append(idx)
    common_types = set(types1.keys()).intersection(set(types2.keys()))

if len(common_types) > 0:
    type_ = random.choice(list(common_types))

    index1 = random.choice(types1[type_])
    index2 = random.choice(types2[type_])

    slice1 = ind1.searchSubtree(index1)
    slice2 = ind2.searchSubtree(index2)
    ind1[slice1], ind2[slice2] = ind2[slice2], ind1[slice1]

return ind1, ind2
    
Return raw Python code only as text, do not wrap it in markdown code blocks or backticks. 
After generating the function, print a brief comment for each line explaining its purpose. Confirm that the variable names are consistent.
